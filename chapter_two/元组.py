# -*- coding:utf-8 -*-
# @author LiuBin
# @date 2025/2/13
# @file 元组.py
"""
元组的定义：
1. 元组是不可变的，一但创建，其元素的值就不能再修改。
2. 元组可以包含多个元素，并且可以嵌套元组。
3. 元组的创建：
   a) 空元组：()
   b) 单元素元组：(1,)
   c) 多个元素元组：(1, 2, 3)
   d) 嵌套元组：((1, 2), (3, 4))
   e) 元组的解包：a, b = (1, 2)
   f) 嵌套元组的解包：a, b, c = ((1, 2), (3, 4), (5, 6))
   g) 嵌套元组的解包：a, b, (c, d) = ((1, 2), (3, 4), (5, 6))
   h) 元组的嵌套：((1, 2), (3, (4, 5)))
   i) 元组的拼接：t1 + t2
   j) 元组的��法：t * 3
   k) 元组的切片：t[start:stop:step]
   l) 元组的反转：reversed(t)
   m) 元组的排序：sorted(t)
   n) 元组的��分：a, b, *c = t
   o) 元组的��分：a, b, *c = t.split(',')
   p) 元组的��分：a, b, *c = t.split(',')
   q) 元组的��分：a, b, *c = t.split(',', 2)
   r) 元组的��分：a, b, *c = t.rsplit(',', 2)
   s) 元组的��分：a, b, *c = t.rsplit(',', 2)
元组的使用注意事项：
1. 元组的元素是不可修改的，一但创建，其元素的值就无法再修改。
2. 元组可以包含多个元素，并且可以��套元组。
3. 元组的创建和使用方法与列表完全相同，但元组是不可变的，一��创建，其元素的值就无法再修改。
4. 元组的元素可以是任何数据类型，包括整型、字符串、布尔值、列表、元组、函数等。

"""
# 元组的定义
t1 = (1,"Hello",True)
t2 = ()
t3 = tuple()
print(t1, type(t1))
t6 = ("传智教育","黑马程序员","Python","Liubin",(11,22,33,44),('a','b','c','d','e','f'),"黑马程序员","黑马程序员")
# 查看某个元素的下标索引
index = t6.index("黑马程序员")
print(index)
# 统计元组中某个元素的出现次数
print(t6.count("黑马程序员"))
# 统计元组元素数量
print(len(t6))

# 元组的遍历
for i in t6:
    print(i, end=" ")
print()
i= 0
while i < len(t6):
    print(t6[i], end=" ")
    i += 1
print()
# 元组的元素不可修改，但元素是list，list内容可修改
t7 = (1, [2, 3], 4)
t7[1][0] = 5
print(t7)

# 元组元素不支持删除,但元素为列表时，可删除列表内容
# del t7[1]   # TypeError: 'tuple' object doesn't support item deletion
# print(t7)
del t7[1][0]
print(t7)